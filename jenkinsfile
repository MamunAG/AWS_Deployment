pipeline {
    agent any
 
    environment {
        registry = '1almamun/aws_docker_example'
        registryCredentialUp = 'docker_up'
        dockerImage = ''
    }

    stages {
        stage('Start') {
            steps {
                echo 'Pipeline start.'
            }
        }

        stage('Deployment') {
            steps {
                echo 'Deployment start.'
            }
        }

        stage('Checkout SCM') {
            steps{
                script{
                    git credentials: 'github_uk',
                    url: 'https://github.com/MamunAG/AWS_Deployment.git',
                    branch: 'main'
                }
            }
        }

        stage('Updating kubernetes deploymnet file') {
            steps{
                script{
                    sh """
                    cat deployment.yaml
                    sed -t 's/${APP_NAME}.*/${APP_NAME}:${IMAGE_TAG}/g' deployment.yaml
                    cat deployment.yaml 
                    """
                }
            }
        }
    }
}
